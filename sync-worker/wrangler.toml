name = "medicine-study-sync"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[[kv_namespaces]]
binding = "STUDY_KV"
id = "REPLACE_WITH_KV_NAMESPACE_ID"

[vars]
# No secrets — auth is client-side hashed; payloads are encrypted client-side.
